<div class="flex items-center justify-center py-4 text-xl font-medium text-white z-100">
  <p>{{ section.settings.text_bar }}</p>
</div>

{% schema %}
  {
    "name": "Announcement Bar",
    "settings": [
      {
        "type": "text",
        "id": "text_bar",
        "label": "Add announcement text",
        "default": "Free Shipping over $100"
      }
    ],
    "presets": [
      {
        "name": "Announcement Bar"
      }
    ]
  }
{% endschema %}