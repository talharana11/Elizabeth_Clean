<div class="flex justify-center items-center py-4 text-white font-medium text-xl">
  <p>{{ section.settings.text_bar }}</p>
</div>

{% schema %}
  {
    "name": "Announcement Bar",
    "settings": [
      {
        "type": "text",
        "id": "text_bar",
        "label": "Add announcement text",
        "default": "Free Shipping over $100"
      }
    ],
    "presets": [
      {
        "name": "Announcement Bar"
      }
    ]
  }
{% endschema %}